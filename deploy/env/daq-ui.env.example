# daq-ui runs in a container but is accessed via nginx/portfolio through localhost mapping.
# The browser hits YOUR domain; daq-ui calls the API via your nginx route (recommended),
# or via localhost if you proxy that way.
NEXT_PUBLIC_API_BASE=
